kind: pipeline
name: default

steps:    
  - name: build
    image: appleboy/drone-ssh:latest
    setting: 
      host: nokeyc
      username: root
      password:
        from_secret: 10.199_SSH_KEY
      port: 22
      script:
        - cd /app
        - ./buildrun.sh
volumes: 
  - name: maven-build
    host: 
      path: /app 
