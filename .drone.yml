kind: pipeline
name: default

steps:    
  - name: build
    image: appleboy/drone-ssh
    username: root
    password: 1qaz2wsx
    volumes:
      - /root/drone_rsa:/root/.ssh/id_rsa.pub
    key_path: /root/.ssh/id_rsa.pub  
    host: drone-server
    port: 22
    command_timeout: 180
    script:
      - cd /app
      - ./buildrun.sh
volumes: 
  - name: maven-build
    host: 
      path: /app 
